[package]
name = "libtls"
version = "0.1.1"
authors = ["Reyk Floeter <contact@reykfloeter.com>"]
edition = "2018"
license = "ISC"
description = "Rust bindings for LibreSSL's libtls library."
repository = "https://github.com/reyk/rust-libtls"

[badges]
travis-ci = { repository = "reyk/rust-libtls", branch = "master" }

[dependencies]
libtls-sys = { path = "../libtls-sys", version = "0.1.1" }

[dev-dependencies]
rand = "0.7.2"
